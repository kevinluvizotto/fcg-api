name: CD - Docker Deploy to Azure

on:
  push:
    branches: [production]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Login no Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Pull da imagem Docker
      - name: Pull da imagem Docker
        run: docker pull klztt/fcg-api:${{ github.sha }}

      # latest 
      - name: Tag imagem como latest
        run: docker tag klztt/fcg-api:${{ github.sha }} klztt/fcg-api:latest

      - name: Push da imagem latest
        run: docker push klztt/fcg-api:latest

      - name: Deploy no Azure App Service
        uses: azure/webapps-deploy@v2
        with:
          app-name: fcg-api-fase2-klztt
          publish-profile: ${{ secrets.AZURE_PUBLISH_PROFILE }}
          images: klztt/fcg-api:latest
